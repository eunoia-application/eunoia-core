<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>ru.eunoia</groupId>
        <artifactId>eunoia-core</artifactId>
        <version>1.0-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <groupId>ru.eunoia.auth</groupId>
    <artifactId>service-auth</artifactId>
    <packaging>pom</packaging>

    <modules>
        <module>auth-api</module>
        <module>auth-logic</module>
        <module>auth-app</module>
    </modules>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <jjwt.version>0.12.6</jjwt.version>
        <mapstruct.version>1.6.3</mapstruct.version>
        <swagger-annotations.version>2.2.25</swagger-annotations.version>
        <openapi-generator.version>7.10.0</openapi-generator.version>
        <eunoia-oas.version>1.0.0</eunoia-oas.version>
    </properties>

    <!--
        No <dependencies> at parent level — each module declares what it needs, so
        auth-logic stays a framework-free core (ref: devpulse-parent). Only versions
        of the auth-specific, non-Boot-managed artifacts are pinned below.
    -->
    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>ru.eunoia.auth.logic</groupId>
                <artifactId>auth-logic</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>ru.eunoia.auth.api</groupId>
                <artifactId>auth-api</artifactId>
                <version>${project.version}</version>
            </dependency>

            <dependency>
                <groupId>org.mapstruct</groupId>
                <artifactId>mapstruct</artifactId>
                <version>${mapstruct.version}</version>
            </dependency>
            <dependency>
                <groupId>org.mapstruct</groupId>
                <artifactId>mapstruct-processor</artifactId>
                <version>${mapstruct.version}</version>
                <scope>provided</scope>
            </dependency>

            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-api</artifactId>
                <version>${jjwt.version}</version>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-impl</artifactId>
                <version>${jjwt.version}</version>
                <scope>runtime</scope>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-jackson</artifactId>
                <version>${jjwt.version}</version>
                <scope>runtime</scope>
            </dependency>

            <dependency>
                <groupId>io.swagger.core.v3</groupId>
                <artifactId>swagger-annotations</artifactId>
                <version>${swagger-annotations.version}</version>
            </dependency>

            <dependency>
                <groupId>com.eunoia.application</groupId>
                <artifactId>shared-contract</artifactId>
                <version>${eunoia-oas.version}</version>
            </dependency>
            <dependency>
                <groupId>com.eunoia.application</groupId>
                <artifactId>auth-contract</artifactId>
                <version>${eunoia-oas.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

</project>
